# Database
# PostgreSQL bağlantı URL'i
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Docker ile çalıştırıyorsanız (docker-compose up):
DATABASE_URL="postgresql://postgres:postgres@db:5432/balik_tutma_rehberi"

# Manuel kurulum için (npm run dev):
# DATABASE_URL="postgresql://kullanici:sifre@localhost:5432/balik_tutma_rehberi"
